---
title: "brighten"
description: "const brighten: (amount?) => (self) => Color & (self, amount?) => Color;"
---
# Variable: brighten

```ts
const brighten: (amount?) => (self) => Color & (self, amount?) => Color;
```

Defined in: [packages/motion/src/Color.ts:241](https://github.com/julia-script/effect-motion/blob/main/packages/motion/src/Color.ts#L241)

Brighten by `amount` steps in Lab space.

## Default Value

`amount` — `1`
