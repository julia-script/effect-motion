---
title: "RenderTarget"
description: "A handle to an offscreen render destination."
---
# Interface: RenderTarget

Defined in: [packages/three/src/RenderTarget.ts:30](https://github.com/julia-script/effect-motion/blob/main/packages/three/src/RenderTarget.ts#L30)

A handle to an offscreen render destination.

## Remarks

The underlying target stays reachable through `~three.renderTarget` for
anything this wrapper does not cover.

## Extends

- `Pipeable`

## Properties

### ~three.renderTarget

```ts
readonly ~three.renderTarget: RenderTarget;
```

Defined in: [packages/three/src/RenderTarget.ts:32](https://github.com/julia-script/effect-motion/blob/main/packages/three/src/RenderTarget.ts#L32)

***

### ~three/RenderTarget

```ts
readonly ~three/RenderTarget: "~three/RenderTarget";
```

Defined in: [packages/three/src/RenderTarget.ts:31](https://github.com/julia-script/effect-motion/blob/main/packages/three/src/RenderTarget.ts#L31)
