---
title: "emitter"
description: "function emitter(props): Effect<EmitterField, never, Runner>;"
---
# Function: emitter()

```ts
function emitter(props): Effect<EmitterField, never, Runner>;
```

Defined in: [packages/motion/src/particles/constructors.ts:75](https://github.com/julia-script/effect-motion/blob/main/packages/motion/src/particles/constructors.ts#L75)

Create a source emitter field. Simulate it with `{ burst: n }` (all at
once) or `{ rate: n }` (a continuous stream).

## Parameters

| Parameter | Type |
| ------ | ------ |
| `props` | [`EmitterInput`](../interfaces/EmitterInput.mdx) |

## Returns

`Effect`\<[`EmitterField`](../type-aliases/EmitterField.mdx), `never`, [`Runner`](../../Runner/classes/Runner.mdx)\>
