---
title: "hex"
description: "function hex(value): Color;"
---
# Function: hex()

```ts
function hex(value): Color;
```

Defined in: [packages/motion/src/Color.ts:128](https://github.com/julia-script/effect-motion/blob/main/packages/motion/src/Color.ts#L128)

Parse a CSS color string — the most convenient constructor.

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `value` | `string` | A CSS color string. |

## Returns

[`Color`](../classes/Color.mdx)

## Remarks

Accepts everything CSS does: `#rgb`, `#rrggbb`, `#rrggbbaa`, named colors
like `"rebeccapurple"`, and functional forms like `rgb()` / `hsl()`.

## Example

```typescript
Color.hex("#7f5af0")
```
