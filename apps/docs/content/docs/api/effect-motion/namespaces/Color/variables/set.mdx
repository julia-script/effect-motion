---
title: "set"
description: "const set: <T>(format, channel, amount) => (self) => Color & <T>(self, format, channel, amount) => Color;"
---
# Variable: set

```ts
const set: <T>(format, channel, amount) => (self) => Color & <T>(self, format, channel, amount) => Color;
```

Defined in: [packages/motion/src/Color.ts:328](https://github.com/julia-script/effect-motion/blob/main/packages/motion/src/Color.ts#L328)
