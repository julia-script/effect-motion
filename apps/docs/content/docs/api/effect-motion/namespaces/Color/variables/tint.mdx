---
title: "tint"
description: "const tint: (amount?, mode?) => (self) => Color & (self, amount?, mode?) => Color;"
---
# Variable: tint

```ts
const tint: (amount?, mode?) => (self) => Color & (self, amount?, mode?) => Color;
```

Defined in: [packages/motion/src/Color.ts:315](https://github.com/julia-script/effect-motion/blob/main/packages/motion/src/Color.ts#L315)
