---
title: "vec3"
description: "const vec3: (input, options?) => ReadonlySide<{"
---
# Variable: vec3

```ts
const vec3: (input, options?) => ReadonlySide<{
  x: withConstructorDefault<Number>;
  y: withConstructorDefault<Number>;
  z: withConstructorDefault<Number>;
}, "Type"> = Vec3.make;
```

Defined in: [packages/motion/src/Entity.ts:49](https://github.com/julia-script/effect-motion/blob/main/packages/motion/src/Entity.ts#L49)

Build a [Vec3](./Vec3-1.mdx); omitted axes are 0.

Constructs a value from the make input representation synchronously.

**When to use**

Use when constructor input is trusted or when validation failure
should abort with a thrown `Error`.

**Details**

Applies constructor defaults and type-side validation according to
`MakeOptions`.

**Gotchas**

Throws an `Error` with the schema issue in its `cause` when validation
fails.
Causes that contain defects, interruptions, or other non-schema reasons
throw with the underlying `Cause` attached instead.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `input` | \{ `x?`: `number`; `y?`: `number`; `z?`: `number`; \} |
| `input.x?` | `number` |
| `input.y?` | `number` |
| `input.z?` | `number` |
| `options?` | `MakeOptions` |

## Returns

`ReadonlySide`\<\{
  `x`: `withConstructorDefault`\<`Number`\>;
  `y`: `withConstructorDefault`\<`Number`\>;
  `z`: `withConstructorDefault`\<`Number`\>;
\}, `"Type"`\>

## See

 - Bottom.makeOption — construct synchronously and discard validation details
 - Bottom.makeEffect — construct through `Effect` when validation failure should stay in the error channel

## Example

```typescript
Entity.vec3({ x: 100, y: 50 })
```
