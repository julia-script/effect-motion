---
title: Springs
description: Physics-based motion with no durations
---

`Physics.springTo` has no duration parameter — each spring runs until it
physically settles. Presets like `"swing"`, `"bounce"`, and `"jump"` trade
stiffness and damping for different characters of motion.

<Example name="springs" />
