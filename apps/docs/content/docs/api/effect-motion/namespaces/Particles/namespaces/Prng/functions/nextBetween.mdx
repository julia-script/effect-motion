---
title: "nextBetween"
description: "function nextBetween("
---
# Function: nextBetween()

```ts
function nextBetween(
   state, 
   min, 
   max): readonly [number, number];
```

Defined in: [packages/motion/src/particles/Prng.ts:45](https://github.com/julia-script/effect-motion/blob/main/packages/motion/src/particles/Prng.ts#L45)

advance the state; returns the next state and a float in [min, max)

## Parameters

| Parameter | Type |
| ------ | ------ |
| `state` | `number` |
| `min` | `number` |
| `max` | `number` |

## Returns

readonly \[`number`, `number`\]
