---
title: "saturate"
description: "const saturate: (amount?) => (self) => Color & (self, amount?) => Color;"
---
# Variable: saturate

```ts
const saturate: (amount?) => (self) => Color & (self, amount?) => Color;
```

Defined in: [packages/motion/src/Color.ts:249](https://github.com/julia-script/effect-motion/blob/main/packages/motion/src/Color.ts#L249)

Increase saturation by `amount` steps.

## Default Value

`amount` — `1`
