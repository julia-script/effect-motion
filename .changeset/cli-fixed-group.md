---
"@effect-motion/cli": patch
---

Add `@effect-motion/cli` to the fixed release group so it versions in lockstep with `effect-motion`, and pick up the fixed `effect-motion` build (runtime `@effect-motion/thorvg` dependency).
