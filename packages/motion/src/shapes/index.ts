// `../src/shapes` (the directory) must resolve on case-SENSITIVE
// filesystems too — without this index it only works where `src/shapes`
// happens to match `src/Shapes.ts` case-insensitively.
export * from "./Shapes";
