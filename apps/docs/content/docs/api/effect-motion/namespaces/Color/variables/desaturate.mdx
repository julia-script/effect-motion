---
title: "desaturate"
description: "const desaturate: (amount?) => (self) => Color & (self, amount?) => Color;"
---
# Variable: desaturate

```ts
const desaturate: (amount?) => (self) => Color & (self, amount?) => Color;
```

Defined in: [packages/motion/src/Color.ts:257](https://github.com/julia-script/effect-motion/blob/main/packages/motion/src/Color.ts#L257)

Reduce saturation by `amount` steps.

## Default Value

`amount` — `1`
