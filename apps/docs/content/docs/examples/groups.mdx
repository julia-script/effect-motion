---
title: Groups
description: Hierarchies move — and fade — as one
---

Children attach to a group and live in its local coordinate space, so a
single motion on the group carries everything inside it. Traits cascade the
same way: `Motion.fadeTo` on the group fades the whole subtree.

<Example name="groups" />
