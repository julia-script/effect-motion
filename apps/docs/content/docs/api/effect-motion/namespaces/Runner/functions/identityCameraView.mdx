---
title: "identityCameraView"
description: "function identityCameraView(width): CameraView & PointOfInterest;"
---
# Function: identityCameraView()

```ts
function identityCameraView(width): CameraView & PointOfInterest;
```

Defined in: [packages/motion/src/Runner.ts:181](https://github.com/julia-script/effect-motion/blob/main/packages/motion/src/Runner.ts#L181)

The resting camera for a comp of the given width: width-relative focal
length (AE's 50mm equivalent), positioned so the z=0 plane projects at
scale 1 and is in focus.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `width` | `number` |

## Returns

`CameraView` & `PointOfInterest`
