---
title: "alpha"
description: "const alpha: (amount) => (self) => Color & (self, amount) => Color;"
---
# Variable: alpha

```ts
const alpha: (amount) => (self) => Color & (self, amount) => Color;
```

Defined in: [packages/motion/src/Color.ts:221](https://github.com/julia-script/effect-motion/blob/main/packages/motion/src/Color.ts#L221)

Set a color's opacity (0–1), leaving its channels untouched.
